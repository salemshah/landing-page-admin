import { Outlet } from 'react-router-dom';

// ==============================|| Auth Layout ||============================== //

const MinimalLayout = () => (
  <>
    <Outlet />
  </>
);

export default MinimalLayout;
